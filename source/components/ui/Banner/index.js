import React from 'react'
import withStyles from 'constructicon/with-styles'
import styles from './styles'

const Banner = ({ classNames }) => <div className={classNames.root} />

export default withStyles(styles)(Banner)
